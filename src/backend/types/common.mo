module {
  public type UserId = Principal;
  public type Timestamp = Int; // nanoseconds from Time.now()
  public type JobId = Nat;
  public type RatingId = Nat;
  public type NotificationId = Nat;
  public type EnterpriseDispatchId = Nat;
  public type PaymentId = Nat;
};
